# Packaging Scripts

Put local packaging/release helpers here.

Planned scripts:
- `stage-portable.sh` — assemble `port/stage/opencode-openbsd/`
- `pack-portable.sh` — produce `.tgz` + `sha256` in `port/release/`
- `smoke-test-portable.sh` — run basic validation against staged bundle
