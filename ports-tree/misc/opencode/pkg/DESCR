OpenCode is a terminal coding assistant with a TUI interface.

This OpenBSD port is currently in a local-bootstrap stage: it installs a locally
built OpenCode binary through the ports framework so install layout, package
metadata, and pkg_add/pkg_delete behavior can be validated in a real ports tree.
